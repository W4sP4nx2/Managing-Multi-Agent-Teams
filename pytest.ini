[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    system: end-to-end multi-agent workflow tests
    api: FastAPI and A2A boundary tests
    notebook: generated notebook execution checks
